# Setup de links simbólicos para a configuração de ambiente
# leia [[ ./README.md ]] para concluir a instalação

# link simbólico para os arquivos bash
ln -s ./bash ~/.config/bash

# link simbólico para os arquivos nvim
ln -s ./nvim ~/.config/nvim

# link simbólico para os arquivos tmux
ln -s ./tmux ~/.config/tmux

